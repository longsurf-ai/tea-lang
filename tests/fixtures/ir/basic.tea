//@version=1
indicator("Basic", overlay=true)

len = input.int(14, "Length", minval=1)
src = input.source(close, "Source")

prev = src[1]
up = close > open

var count = 0
if up
	count := count + 1

delta = close - prev
plot(delta, "Delta", color=color.blue)
