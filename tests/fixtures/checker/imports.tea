//@version=1


// Builtin libraries need no import; ta is bound by the implicit import.
s1 = ta.sma(close, 3)

// An explicit import of a builtin is a legal no-op.
import ta

s2 = ta.sma(close, 3)
emit "output0" s1 - s2

import nosuchlib // ERROR unknown library 'nosuchlib'

import someone/lib/1 // ERROR external libraries are not supported yet
