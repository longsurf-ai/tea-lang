{
  "name": "tea-typegpu-backtest-experiment",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "license": "MIT",
  "scripts": {
    "build": "vite build",
    "typecheck": "tsc --noEmit",
    "test": "npm run build && node --import tsx --test test/*.case.ts",
    "capability-smoke": "npm run build --silent && node dist/cli.js capability-smoke",
    "parity-smoke": "npm run build --silent && node dist/cli.js parity-smoke",
    "download": "npm run build --silent && node dist/cli.js download",
    "sweep": "npm run build --silent && node dist/cli.js sweep",
    "artifacts": "npm run build --silent && node dist/cli.js artifacts",
    "benchmark": "npm run build --silent && node dist/cli.js benchmark"
  },
  "dependencies": {
    "typegpu": "0.11.9",
    "webgpu": "0.4.0",
    "zod": "4.4.3"
  },
  "devDependencies": {
    "@types/node": "25.0.10",
    "@webgpu/types": "0.1.71",
    "tsx": "4.23.10",
    "typescript": "5.8.3",
    "unplugin-typegpu": "0.11.6",
    "vite": "8.2.1"
  },
  "engines": {
    "node": ">=22.12.0 <23"
  }
}
