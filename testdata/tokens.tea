//@version=1
var float b = 1.4
x = close + open * 2
s = "hi" + 'there'
c1 = #ff0000
c2 = #FF00AA80
if x > b and not na(x)
    y := x % 3
    z = x >= 2 ? 1 : 0
else
    w = f(a, offset = .5)
plot(x,
  color = c1)
h = nums[1]
t = x <= 2 or x != 3
u = x / 2 - 1
v = 1e10 + 2.5e-3
m += 1
g() =>
    1
/* block
   comment */
type Foo
    int q
    float r
