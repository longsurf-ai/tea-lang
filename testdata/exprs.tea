//@version=1
x = 1
var float b = 1.4
varip cnt = 0
const model = "gpt"
y := x + 2
y += 3
[a, c] = f(x, b)
h = x >= 0 ? b > 1.0 ? 1 : 2 : 3
s = "hi" + 'there'
col = #26a69a
n = not (x == 2) and y != 4 or b < .5
d = close[1] - open[x + 1]
sel = ta.ema(close, 12)
p = plot(d, title = "D", color = col)
xs = array.new<float>(0)
float[] fs = array.from(1.0, 2.0)
m = math.max(a, c)[2]
u = -close[1] * 2 % 3 / 4
t = [x, y]
q = a < b
